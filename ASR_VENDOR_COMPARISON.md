# ASR 厂商能力与参数对比

> 面向评估汇报与厂商选型。当前对比统一采用实时流式识别；阿里云统一使用 `paraformer-realtime-v2`，不考虑其他旧模型。

## 一、三家厂商能力对比

| 对比维度 | Stepfun | 豆包 / 火山引擎 | 阿里云 |
| --- | --- | --- | --- |
| 当前使用模型 | `stepaudio-2.5-asr-stream` | 豆包流式语音识别大模型 | `paraformer-realtime-v2` |
| 产品特点 | 接入简单，流式响应快 | 可调参数较多，兼顾实时性和最终准确率 | 语种控制清晰，适合普通话、粤语及多语种场景 |
| 实时流式识别 | 支持 | 支持 | 支持 |
| 普通话识别 | 支持 | 支持 | 支持 |
| 粤语识别 | 支持，目前实测效果较好 | 支持，目前实测效果较好 | 支持，可自动识别或提示粤语 |
| 自动识别语种 | 在中文范围内由模型识别，当前没有单独返回语种 | 支持语种检测，可返回识别出的语种 | 支持，不传语种提示时由模型自动判断 |
| 明确指定粤语 | 当前接口没有单独传粤语参数 | `enable_lid` 用于检测语种，不是强制指定粤语 | 可通过 `language_hints=["yue"]` 提示模型按粤语识别 |
| 流式中间结果 | 支持 | 支持 | 支持 |
| 最终结果优化 | 支持整句二次识别，提高最终文本准确率 | 支持二遍识别，先快速返回，再输出高精度结果 | 模型持续修正并输出最终结果 |
| 自动断句 | 支持 | 支持 | 支持 |
| 断句灵活度 | 可调整静音时间和 VAD 灵敏度 | 可调整静音时间、最短语音时长，控制较丰富 | 可调整句尾静音时间 |
| 数字转换 | 支持，例如“一百”转为“100” | 支持 | 支持 |
| 自动标点 | 支持 | 支持，可单独开关 | 支持 |
| 口语顺滑 | 当前没有单独开关 | 支持删除“嗯、啊”等语气词 | 主要依赖模型处理 |
| 简体 / 繁体输出 | 当前没有单独配置 | 可选择简体、繁体、香港繁体、台湾正体 | 可结合模型能力和后处理实现 |
| 热词 / 专业词 | 厂商能力需结合正式接口确认，当前项目尚未接入 | 厂商支持，当前项目尚未接入 | 厂商支持，当前项目尚未接入 |
| 参数灵活度 | 中等 | 高 | 中高 |
| 当前主要优势 | 接入简单，粤语实测稳定，响应较快 | 调优空间较大，粤语效果较好 | 可以明确提示粤语，语种控制能力更清晰 |
| 当前关注点 | 中文参数没有进一步区分普通话和粤语 | 参数较多，评测时必须固定配置 | 自动识别和提示粤语可能产生不同结果，需要分别评估 |

## 二、差异化可调参数对比

以下仅列出三家当前接入的流式 ASR 接口中，参数能力或参数语义存在差异的项目；三家均具备的模型选择、音频格式/采样率、静音判停和文本数字规范化（ITN）不再重复列出。`—` 表示当前接口文档未提供对应的独立开关，并不等同于产品完全没有该能力。

| 差异项 | Stepfun `stepaudio-2.5-asr-stream` | 豆包 / 火山引擎 BigASR `bigmodel_async` | 阿里云 `paraformer-realtime-v2` |
| --- | --- | --- | --- |
| 语种控制方式 | `language` 直接指定识别语言；项目当前为 `zh` | `enable_lid` 仅开启语种检测，并在结果中返回语种，不是强制按粤语识别 | `language_hints` 为语言提示；可传 `yue` 提示粤语，留空由模型自行判断 |
| VAD 灵敏度 | `threshold`：单独调节 VAD 判定严格度 | — | — |
| 短语音防过早切分 | — | `force_to_speech_time`：设定最小有效语音时长后再允许判停 | — |
| 二遍纠错 / 最终结果增强 | `full_rerun_on_commit`：提交后进行二遍识别纠错 | `enable_nonstream`：流式快速结果后，返回高精度最终结果 | 无同类独立开关；接口按自身流式最终结果输出 |
| 流式结果形态 | 通过 `delta` / `completed` 事件分别接收增量与最终文本 | `result_type` 可选全量或增量返回 | 接口返回整句持续修正结果；当前接口未暴露同类切换项 |
| 标点独立开关 | — | `enable_punc` | — |
| 语气词 / 口语顺滑 | — | `enable_ddc`：删除“嗯、啊”等语气词 | — |
| 中文书写变体 | — | `output_zh_variant`：可选简体、繁体、香港繁体 `hk`、台湾正体 `tw` | — |
| 字级时间戳精对齐 | `enable_timestamp_align`：仅该模型生效，开启后在最终结果返回更精确的字级时间戳 | — | — |

本项目当前开启：Stepfun 的 `full_rerun_on_commit`；火山的 `enable_punc`、`enable_ddc`、`enable_nonstream`、`enable_lid`，并设置 `force_to_speech_time`；阿里云的 `language_hints` 留空。比较准确率时，上述开关会改变最终文本，必须将其随 Run 一同记录。

官方接口文档（本次核对）：[Stepfun 流式 ASR](https://platform.stepfun.com/docs/zh/api-reference/audio/asr-stream)、[火山引擎语音技术文档](https://www.volcengine.com/docs/6561/80818)、[阿里云百炼模型服务文档](https://help.aliyun.com/zh/model-studio/)。厂商会调整接口字段与支持范围，变更配置前应以对应账号控制台展示的最新文档为准。

## 三、当前项目的实际评测配置

| 厂商 | 当前配置 | 对评测结果的影响 |
| --- | --- | --- |
| Stepfun | 中文识别、数字转换开启、整句二次识别开启、静音约 800ms 断句 | 最终结果会经过整句修正；断句速度适中 |
| 豆包 / 火山引擎 | 数字转换、标点、口语顺滑、二遍识别、语种检测均开启，静音约 500ms 断句 | 断句相对积极；最终结果经过二遍识别和口语顺滑处理 |
| 阿里云 | `paraformer-realtime-v2`、16kHz 音频、静音约 1300ms 断句，语种提示留空 | 当前由模型自动判断语种；停顿容忍时间相对较长 |

## 四、评测建议

| 评测事项 | 建议 |
| --- | --- |
| 普通话评测 | 三家使用相同音频和相同参考文本，直接横向比较 |
| 粤语主评测 | 三家统一采用自动识别模式，作为公平对比的主要结果 |
| 阿里云粤语增强评测 | 补充一组 `language_hints=["yue"]` 的结果，观察明确提示粤语后的提升幅度 |
| 配置一致性 | 每次 Run 记录模型、语种模式和关键识别参数，避免配置变化导致结果不可比较 |
| 核心指标 | 同时关注准确率、首包速度、最终延迟、长语音完整性和运行稳定性 |
| Case 设计 | 普通话和粤语分组统计，并覆盖短句、长句、停顿、数字、专有名词及噪声场景 |

## 五、最新实测结果（2026-07-15）

本节基于以下两份已完成报告，三家均使用宽松文本模式，case 的 CER 阈值均为 `0.2`。

| 任务 | 报告 ID | 样本构成 |
| --- | --- | --- |
| 常规评估任务 | `run_5170d80a-4785-4362-9693-db1be037b4c1` | 46 条常规 case |
| 抗噪评估任务 | `run_0a61a822-9d30-40c4-8972-d7c385a98915` | 29 条干净粤语源 case + 87 条加噪副本（每条 3 种噪声） |

### 5.1 常规评估

| 厂商 | 通过数 / 46 | 通过率 | 平均 CER | 平均 WER | 首包时延 | 最终时延 | 实体准确率 | case 胜出数 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Stepfun | 31 | 67.4% | 0.278 | 0.360 | 566 ms | 7,263 ms | 79.2% | 5 |
| 豆包 / 火山引擎 | 32 | 69.6% | 0.244 | 0.301 | 2,157 ms | 6,839 ms | 79.2% | 33 |
| 阿里云 | 32 | 69.6% | **0.199** | **0.224** | 1,559 ms | 7,151 ms | 70.8% | 8 |

- 常规准确率上，火山与阿里云并列第一；在通过率相同的情况下，阿里云的平均 CER、WER 更低。
- 时延上，Stepfun 首包最快；火山最终时延最低。
- “case 胜出”按单 case 的通过状态、CER、最终时延依次排序；它不等同于总体通过率。

### 5.2 抗噪评估

整体通过率包含干净源 case 与加噪副本，不能直接用来衡量抗噪性。以下抗噪指标仅使用“同一源 case 的干净音频—加噪音频”有效配对：Stepfun、阿里云各 87 对；火山有 1 条加噪结果超时，故为 86 对。

| 厂商 | 整体通过数 / 116 | 整体通过率 | 有效配对 | 通过保持 | CER 增量 | 抗噪指数 |
| --- | --- | --- | --- | --- | --- | --- |
| Stepfun | 43 | 37.1% | 87 | 42.1%（24 / 57） | +0.324 | 29.5% |
| 豆包 / 火山引擎 | 63 | **54.3%** | 86 | **68.3%（41 / 60）** | **+0.188** | **49.6%** |
| 阿里云 | 57 | 49.1% | 87 | 63.2%（36 / 57） | +0.250 | 44.2% |

抗噪指数 = `70% × 通过保持率 + 30% × CER 稳定度`；CER 稳定度随 CER 增量从 0 到 0.2 线性下降，增量达到或超过 0.2 时该项为 0。因此该指数用于比较相同噪声集下的相对抗噪表现，不应替代实际通过率。

| 噪声档位 | Stepfun：通过保持 / CER 增量 | 豆包 / 火山引擎：通过保持 / CER 增量 | 阿里云：通过保持 / CER 增量 |
| --- | --- | --- | --- |
| 白噪 20 dB（轻度） | 47.4% / +0.278 | 85.0% / +0.050 | **94.7% / +0.019** |
| 粉红噪 10 dB（中度） | 52.6% / +0.215 | 85.0% / +0.014 | **89.5% / +0.113** |
| 背景人声 5 dB（重度） | 26.3% / +0.481 | **35.0% / +0.497** | 5.3% / +0.619 |

- 火山在整个抗噪集的通过保持、平均 CER 增量和抗噪指数上均居首，综合抗噪能力最强。
- 阿里云在白噪和粉红噪下保持最好，但在背景人声 5 dB 下明显失速；该档位是三家共同的主要短板。
- 本次历史报告未持久化到单 case 的实体准确率，因此不展示“实体保持率”；待后续报告具备该字段后再补齐，不以推测值替代。

## 六、汇报结论

1. 三家厂商均支持实时流式识别，可以在同一评估看板中进行横向比较。
2. 常规任务中，火山和阿里云的通过率均为 69.6%；阿里云的平均 CER/WER 最低，Stepfun 的首包时延最快。
3. 抗噪任务中，火山的通过保持率为 68.3%、抗噪指数为 49.6%，当前综合抗噪表现最佳；阿里云为 63.2% 和 44.2%，Stepfun 为 42.1% 和 29.5%。
4. 背景人声 5 dB 对三家影响最大，当前不宜仅凭白噪或粉红噪结果推断真实嘈杂对话场景的表现。
5. 阿里云统一使用 `paraformer-realtime-v2`。除自动识别外，可增加粤语提示模式，用于判断语种提示能带来多大提升。
6. 最终选型仍需结合准确率、抗噪性、首包速度、最终时延、稳定性和参数维护成本；若以当前两份报告为依据，火山是更均衡的优先候选。

## 七、口径说明

- “厂商支持”表示接口或模型具备对应能力；“当前项目已启用”表示现有 Java 接入代码已经传入对应参数，两者不可混为一谈。
- 三家厂商的二次识别、断句和文本规范化策略不同。正式汇报时应同时展示原始指标和配置快照。
- 厂商接口能力可能随模型版本和服务配置变化，上线或正式采购前应以对应账号下的最新接口文档和实测结果为准。
- 抗噪“通过保持”只以干净音频原本通过的配对为分母；“CER 增量”则以全部有效配对计算。两者回答的问题不同，不能混用。
